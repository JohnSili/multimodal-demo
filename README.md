# SmolVLM2 Web Demo

Веб-приложение для демонстрации возможностей мультимодальной модели SmolVLM2. Предоставляет интерфейс для Visual Question Answering (VQA), автоматического описания изображений и OCR (распознавания текста).

## Возможности

- **Visual Question Answering**: Задавайте вопросы об изображениях и получайте детальные ответы
- **Image Captioning**: Автоматическое создание описаний изображений
- **OCR**: Распознавание текста на изображениях (английский и русский языки)
- **Сессии**: Сохранение изображений в сессии для множественных вопросов без повторной загрузки
- **Скачивание результатов**: Экспорт распознанного текста в .txt файл

## Требования к системе

- **Docker** и **Docker Compose v2**
- Минимум **8GB RAM** для CPU версии
- Минимум **6GB VRAM** для GPU версии (опционально)
- **NVIDIA Docker** для GPU поддержки (опционально)

> 📖 Подробная инструкция по настройке GPU: [GPU_SETUP.md](GPU_SETUP.md)

## Быстрый старт

### Способ 1: Использование скриптов (рекомендуется)

**Для CPU:**
```bash
./start.sh cpu
```

**Для GPU:**
```bash
./start.sh gpu
```

**Только сборка без запуска:**
```bash
./build.sh cpu    # Сборка CPU версии
./build.sh gpu    # Сборка GPU версии
```

### Способ 2: Прямое использование Docker Compose

**CPU версия:**
```bash
docker-compose up -d --build
```

**GPU версия:**
```bash
docker-compose -f docker-compose.gpu.yml up -d --build
```

### Способ 3: Через переменную окружения DOCKERFILE

**CPU версия (по умолчанию):**
```bash
docker-compose up -d --build
```

**GPU версия:**
```bash
DOCKERFILE=Dockerfile.gpu docker-compose up -d --build
```

### Настройка .env файла

Перед запуском создайте файл `.env`:

**Для CPU:**
```bash
DEVICE=cpu
TORCH_DTYPE=float32
```

**Для GPU:**
```bash
DEVICE=cuda
TORCH_DTYPE=bfloat16
```

### Проверка GPU (только для GPU версии)

Перед запуском GPU версии убедитесь, что NVIDIA Docker установлен:
```bash
docker run --rm --gpus all nvidia/cuda:12.1.0-base-ubuntu22.04 nvidia-smi
```

### Открытие приложения

После запуска откройте в браузере:
```
http://localhost:8000
```

При первом запуске модель будет автоматически загружена из HuggingFace Hub. Это может занять некоторое время в зависимости от скорости интернета.

## Конфигурация

Все настройки приложения находятся в файле `.env`. Основные параметры:

### Настройки модели

- `MODEL_NAME`: Имя модели на HuggingFace (по умолчанию: `HuggingFaceTB/SmolVLM2-2.2B-Instruct`)
- `MODEL_SIZE`: Размер модели - `instruct` или `base`
- `DEVICE`: Устройство - `cpu` или `cuda`
  - Для CPU: используйте `cpu` с `TORCH_DTYPE=float32`
  - Для GPU: используйте `cuda` с `TORCH_DTYPE=bfloat16` (рекомендуется) или `float16`
- `TORCH_DTYPE`: Тип данных - `float16`, `bfloat16` или `float32`
  - `bfloat16`: Рекомендуется для GPU (лучшая стабильность)
  - `float16`: Меньше памяти, но может быть менее стабильным
  - `float32`: Только для CPU

### Настройки сервера

- `PORT`: Порт для доступа к приложению (по умолчанию: `8000`)
- `WORKERS`: Количество воркеров uvicorn (по умолчанию: `1`)

### Лимиты

- `MAX_IMAGE_SIZE`: Максимальный размер изображения в байтах (по умолчанию: 10MB)
- `MAX_IMAGE_DIMENSION`: Максимальное разрешение изображения (по умолчанию: 4096px)
- `SESSION_TIMEOUT`: Таймаут сессии в секундах (по умолчанию: 3600 = 1 час)

## Использование API

### Health Check

```bash
curl http://localhost:8000/api/health
```

### Visual Question Answering

```bash
curl -X POST http://localhost:8000/api/vqa \
  -H "Content-Type: application/json" \
  -d '{
    "image": "base64_encoded_image_here",
    "question": "Что изображено на картинке?",
    "session_id": "optional_session_id"
  }'
```

### OCR

```bash
curl -X POST http://localhost:8000/api/ocr \
  -H "Content-Type: application/json" \
  -d '{
    "image": "base64_encoded_image_here",
    "language": "en"
  }'
```

### Скачивание результата OCR

```bash
curl http://localhost:8000/api/download/ocr/{task_id} -o result.txt
```

## Структура проекта

```
ProjectPTAI/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py          # FastAPI приложение
│   │   ├── config.py        # Конфигурация
│   │   ├── schemas.py       # Pydantic схемы
│   │   ├── validators.py    # Валидация данных
│   │   ├── utils.py         # Вспомогательные функции
│   │   └── model_manager.py # Менеджер модели
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── index.html
│   ├── style.css
│   └── app.js
├── docs/
│   └── MODEL_DESCRIPTION.md
├── models/                  # Volume для весов моделей (не в git)
├── docker-compose.yml
├── .env.example
├── .gitignore
└── README.md
```

## Развертывание

### Локальная сборка образа

**CPU версия:**
```bash
docker build -t smolvlm2-backend:cpu -f ./backend/Dockerfile ./backend
```

**GPU версия:**
```bash
docker build -t smolvlm2-backend:gpu -f ./backend/Dockerfile.gpu ./backend
```

Или используйте скрипт:
```bash
./build.sh cpu    # Сборка CPU версии
./build.sh gpu    # Сборка GPU версии
```

### Использование готового образа из Docker Hub

Если образ опубликован в Docker Hub:

```bash
docker pull your-username/smolvlm2-backend
```

Затем измените `docker-compose.yml` для использования готового образа.

### GPU поддержка

Для использования GPU есть несколько способов:

1. **Использовать скрипт (рекомендуется):**
   ```bash
   ./start.sh gpu
   ```

2. **Использовать отдельный compose файл:**
   ```bash
   docker-compose -f docker-compose.gpu.yml up -d --build
   ```

3. **Использовать переменную окружения:**
   ```bash
   DOCKERFILE=Dockerfile.gpu docker-compose up -d --build
   ```

В `.env` установите:
```bash
DEVICE=cuda
TORCH_DTYPE=bfloat16
```

> 📖 Подробная инструкция: [GPU_SETUP.md](GPU_SETUP.md)

## Troubleshooting

### Недостаток памяти

Если приложение падает с ошибкой нехватки памяти:
- Уменьшите `TORCH_DTYPE` до `float16` или `bfloat16`
- Используйте GPU версию
- Увеличьте лимиты памяти Docker

### Проблемы с GPU

- Убедитесь, что установлен NVIDIA Docker: `nvidia-docker`
- Проверьте доступность GPU: `nvidia-smi`
- Убедитесь, что в `docker-compose.yml` раскомментирована секция GPU

### Ошибки загрузки модели

- Проверьте подключение к интернету (для первой загрузки)
- Убедитесь, что volume `./models` правильно смонтирован
- Проверьте права доступа к директории `models/`

### Медленная работа

- Используйте GPU для ускорения инференса
- Уменьшите `MAX_IMAGE_DIMENSION` для обработки меньших изображений
- Используйте `float16` вместо `float32` для экономии памяти и ускорения

## Производительность

- **Время первой загрузки модели**: ~2-5 минут (зависит от скорости интернета)
- **Среднее время inference на CPU**: 5-15 секунд на изображение
- **Среднее время inference на GPU**: 1-3 секунды на изображение
- **Memory footprint**: ~4-6GB RAM для CPU, ~6-8GB VRAM для GPU

## Лицензия

См. документацию модели SmolVLM2 в `docs/MODEL_DESCRIPTION.md`.

## Ссылки

- [SmolVLM2 на HuggingFace](https://huggingface.co/HuggingFaceTB/SmolVLM2-Instruct)
- [Документация модели](./docs/MODEL_DESCRIPTION.md)
- [FastAPI документация](https://fastapi.tiangolo.com/)

## Поддержка

При возникновении проблем создайте issue в репозитории проекта.

